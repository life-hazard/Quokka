<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.quokka">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quokka">
        <activity android:name="com.app.quokka.tasks.FullShowTaskActivity"></activity>
        <activity android:name="com.app.quokka.tasks.FullUserTaskActivity" />
        <activity android:name="com.app.quokka.logged.UserProfileEditActivity" />
        <activity android:name="com.app.quokka.tasks.FullTaskActivity" />
        <activity android:name="com.app.quokka.logged.AddTaskActivity" />
        <activity android:name="com.app.quokka.unlogged.LogInActivity" />
        <activity android:name="com.app.quokka.unlogged.SignUpActivity" />
        <activity
            android:name="com.app.quokka.logged.UserProfileActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.app.quokka.logged.UserHomePageActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="com.app.quokka.MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>