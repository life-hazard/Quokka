<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.quokka">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quokka">
        <activity android:name=".tasks.DoneTaskActivity"></activity>
        <activity android:name=".logged.FinishedTasksActivity" />
        <activity android:name=".tasks.FullShowTaskActivity" />
        <activity android:name=".tasks.FullUserTaskActivity" />
        <activity android:name=".logged.UserProfileEditActivity" />
        <activity android:name=".tasks.FullTaskActivity" />
        <activity android:name=".logged.AddTaskActivity" />
        <activity android:name=".unlogged.LogInActivity" />
        <activity android:name=".unlogged.SignUpActivity" />
        <activity
            android:name=".logged.UserProfileActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".logged.UserHomePageActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>