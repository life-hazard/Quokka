<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/logInActivity">

    <fragment
        android:id="@+id/availableTasksBrowser"
        android:name="com.app.quokka.logged.AvailableTasksBrowser"
        android:label="fragment_available_tasks_browser"
        tools:layout="@layout/fragment_available_tasks_browser" >
    </fragment>

    <fragment
        android:id="@+id/inProgressFragment"
        android:name="com.app.quokka.logged.InProgressFragment"
        android:label="InProgressFragment" >

    </fragment>
    <fragment
        android:id="@+id/userTasksFragment"
        android:name="com.app.quokka.logged.UserTasksFragment"
        android:label="UserTasksFragment" >
    </fragment>
    <activity
        android:id="@+id/logInActivity"
        android:name="com.app.quokka.unlogged.LogInActivity"
        android:label="LogInActivity" />
</navigation>